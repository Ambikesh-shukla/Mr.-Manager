My Discord bot is already built, but it has two major problems that must be fixed professionally:

1. Some slash commands return "command not found"
2. The ticket setup system is too confusing and command-heavy

I want you to REFACTOR and FIX the existing bot, not rebuild random features from scratch.

MAIN GOAL:
Make the bot feel simple, polished, and user-friendly like premium bots such as Ticket Tool / Titcket-style setup systems.

==================================================
PART 1 — FIX "COMMAND NOT FOUND" ISSUE
==================================================

Find and fix all causes of slash commands not appearing or returning "command not found".

Check for these issues and repair them properly:
- Slash commands not being registered correctly
- Global vs guild command registration problems
- Missing REST deployment script
- Command files not loading from folder properly
- Wrong command export structure
- Broken command handler
- Duplicate command names
- Invalid slash command builder usage
- Bot missing application.commands scope
- Command deployment not refreshing after edits
- Startup load errors silently failing
- Incorrect intents or partials interfering with command handling
- Wrong interactionCreate event logic
- Command collection not being initialized correctly

You must:
- Audit the current command system
- Fix all registration logic
- Ensure commands load dynamically from folders
- Log command loading clearly in console
- Log deployment success/failure clearly
- Add a deploy-commands script if missing
- Ensure all commands work after restart
- Ensure no valid command shows "command not found"

Also:
- Make command handling robust with proper error replies
- If a command crashes, reply with a friendly ephemeral error
- Prevent silent failures

==================================================
PART 2 — REDESIGN THE TICKET SETUP UX
==================================================

The current ticket system is too confusing because it uses too many setup commands.
I do NOT want 15-18 different setup commands for basic ticket creation.

I want a MUCH simpler setup flow like this:

/setup-ticket

When an admin runs /setup-ticket:
- A setup embed/panel should appear
- It should contain multiple buttons and clean sections
- Admin should be able to configure the whole ticket panel visually from there

Required buttons or menu options inside the setup panel:
- Set Title
- Set Description
- Set Color
- Set Footer
- Set Thumbnail
- Set Banner Image
- Set Category
- Set Support Roles
- Set Log Channel
- Set Transcript Channel
- Set Ticket Name Format
- Add Button Ticket Type
- Add Dropdown Ticket Type
- Edit Existing Type
- Remove Type
- Toggle Modal Questions
- Set Cooldown
- Set Max Open Tickets
- Preview Panel
- Create Panel
- Cancel Setup

IMPORTANT UX RULE:
The setup must feel interactive and visual.
Do NOT force admins to remember lots of separate commands.
Most ticket configuration should happen through:
- buttons
- select menus
- modals
- preview embeds

==================================================
PART 3 — HOW THE NEW TICKET SETUP SHOULD WORK
==================================================

Desired flow:

Step 1:
Admin runs /setup-ticket

Step 2:
Bot sends an ephemeral or admin-only interactive setup panel

Step 3:
Admin clicks buttons like:
- Set Title → modal opens
- Set Description → modal opens
- Add Button Ticket Type → modal opens
- Set Support Roles → role select menu opens
- Set Category → channel/category select menu opens
- Set Log Channel → channel select
- Preview Panel → shows final result
- Create Panel → posts the final ticket panel in selected channel

This setup should be easy even for a beginner server owner.

==================================================
PART 4 — TICKET TYPES UX
==================================================

Ticket types must be manageable from the setup panel.

Admin should be able to add ticket types like:
- Purchase
- Support
- Bug Report
- Partnership
- Staff Application

Each type should support:
- Name
- Emoji
- Description
- Category override
- Support role override
- Modal questions optional
- Button or dropdown mode

For button mode:
- multiple buttons can be added

For dropdown mode:
- multiple dropdown options can be added

==================================================
PART 5 — KEEP THE ADVANCED FEATURES, BUT HIDE COMPLEXITY
==================================================

Do NOT remove advanced features.
Keep them, but move complexity behind the interactive setup UI.

Keep features like:
- ticket owner can close own ticket
- admin/support can close tickets
- claim/unclaim
- cooldown after close
- ticket limits
- transcripts
- logs
- add/remove user
- rename ticket
- reopen ticket if enabled
- permission checks
- purchase/support ticket types

But the admin should not need tons of setup commands to use them.

==================================================
PART 6 — IMPROVE COMMAND DESIGN
==================================================

Keep commands minimal and clean.

Good command structure example:
- /setup-ticket
- /ticket close
- /ticket claim
- /ticket add-user
- /ticket remove-user
- /ticket rename
- /ticket info
- /ticket reopen
- /panel list
- /panel delete
- /plan create
- /plan list
- /review setup
- /config view

Avoid bloated command spam.
Use subcommands where appropriate.

==================================================
PART 7 — TECHNICAL REQUIREMENTS
==================================================

- Refactor the existing code, do not produce fake pseudo-code
- Keep the bot optimized and modular
- Keep local JSON/file storage if already using that
- Do not reintroduce MongoDB unless explicitly required
- Reuse existing logic where possible
- Remove duplicate or dead code
- Centralize ticket config logic
- Centralize interaction handlers
- Clean up button/select/modal customId naming
- Make all customIds structured and easy to debug
- Prevent interaction conflicts
- Ensure collectors are cleaned properly
- Ensure setup sessions do not break if multiple admins configure different panels
- Store unfinished setup state safely per guild/admin if needed

==================================================
PART 8 — DEBUGGING + STABILITY
==================================================

Add proper logs for:
- command loading
- command deployment
- interaction received
- ticket setup actions
- ticket creation
- ticket closing
- command errors
- missing config errors

Fix any issues causing:
- commands not registering
- interactions failing silently
- setup buttons not responding
- modals not submitting correctly
- select menus not applying values
- posted panel not working after restart

==================================================
PART 9 — FINAL RESULT I WANT
==================================================

After the refactor, I want this experience:

1. Admin types /setup-ticket
2. Bot opens one clean interactive setup panel
3. Admin configures everything through buttons, modals, and selects
4. Admin previews the final panel
5. Admin clicks Create Panel
6. Ticket panel gets posted and works properly
7. All commands are registered and no longer show "command not found"

==================================================
PART 10 — OUTPUT FORMAT
==================================================

Provide:
1. Updated folder structure
2. Fixed command handler code
3. Fixed command deployment code
4. Refactored /setup-ticket command
5. Interaction handler for buttons/selects/modals
6. Ticket setup state manager
7. Any updated utility/storage files
8. Explanation of what was broken and what was fixed
9. Setup steps to test all commands
10. Notes for Replit deployment

IMPORTANT:
Do not simplify by removing core features.
Do not make the ticket system smaller by making it weaker.
Make it easier to use while keeping it powerful.